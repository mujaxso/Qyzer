use iced::Element;
use crate::app::Message;

#[allow(dead_code)]
pub fn terminal<'a>() -> Element<'a, Message> {
    // Placeholder for terminal UI
    iced::widget::text("Terminal placeholder").into()
}
